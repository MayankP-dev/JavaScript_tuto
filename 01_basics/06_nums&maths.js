const score = 400
const balance = new Number(100)
// console.log(score);

// console.log(balance);

console.log(balance.toString()); //converts to string 
console.log(balance.toFixed(2)); //gives precision value, till decimal point

const hundreds = 1000000
// console.log(hundreds.toLocaleString());

/* ----------------------Maths---------------*/

console.log(Math.abs(-4)); //changes the -ve to +ve
console.log(Math.round(4.6)); //rounds off to nearest integer 
console.log(Math.random()); //creates random number between 0-1

const min = 10
const max = 20

console.log(Math.floor(Math.random() * (max-min + 1)) + min)



